{
    "commands": {
        "test": {
            "description": "Test command SLASH and PREFIX of Toatrica.",
            "options": [
                {
                    "error": "The `{{argument}}` is not a `{{type}}`." 
                }
            ]
        }
    },
    "handlers": {
        "commands": {
            "permissions": {
                "client": "I need to have {{permissions}} permissions to run this command.",
                "member": "You need {{permissions}} permissions to use this command."
            }
        },
        "flags": {
            "member": "member",
            "user": "user",
            "string": "string",
            "role": "role",
            "text_channel": "text channel",
            "guild_channel": "guild channel",
            "boolean": "boolean",
            "attachment": "attachment"
        }
    }
}